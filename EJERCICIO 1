#include <iostream>
#include <string>
using namespace std;

int main(){
    string txt, invertida="";
    cout << "Ingrese texto: ";
    getline(cin, txt);

    for(int i = txt.length()-1; i >= 0; i--){
        invertida = invertida + txt[i];
    }

    cout << invertida;
    return 0;
}
